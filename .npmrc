# GitHub Packages registry — scope this to your GitHub org/username
# Replace YOUR_GITHUB_ORG with your actual GitHub username or org (must match package.json "name" scope)
@YOUR_GITHUB_ORG:registry=https://npm.pkg.github.com

# Auth token — do NOT commit a real token. Use an env var instead:
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
